// CONVERSIONES EXPLICITAS A STRING
const numero=8;
const verdadero=true;
const falso=false;
const nulo=null;
let indefinido;

console.log(`Conversion de`, numero,` a string: ${String(numero)}`);
console.log(`Conversion de`, verdadero,` a string: ${String(verdadero)}`);
console.log(`Conversion de`, falso,` a string: ${String(falso)}`);
console.log(`Conversion de`, nulo,` a string: ${String(nulo)}`);
console.log(`Conversion de`, indefinido,` a string: ${String(indefinido)}`);